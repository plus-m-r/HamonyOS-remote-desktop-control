import 'package:flutter/material.dart';

/// 设置页：占位，后续补充。
class SettingsPage extends StatelessWidget {
  const SettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('设置（待实现）'));
  }
}
