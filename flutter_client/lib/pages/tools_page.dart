import 'package:flutter/material.dart';

/// 工具页：占位，后续补充。
class ToolsPage extends StatelessWidget {
  const ToolsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('工具（待实现）'));
  }
}
